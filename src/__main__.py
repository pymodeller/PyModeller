from pymodeller.cli.commands import codegen
# from config_das.config_models import get_config_das_settings

if __name__ == "__main__":
    codegen()
    # values = get_config_das_settings()
    # print(values.device.name)
    # print(values._metadata_origins)
